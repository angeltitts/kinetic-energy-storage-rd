# Results

`baseline.json` is the committed deterministic Phase 0 baseline.

`phase0_sweep.csv` is generated by `python models/parameter_sweep.py` and uploaded by the Phase 0 GitHub Actions workflow as an artifact. It is intentionally not committed because it is a generated sweep output.
